version: '3.8'
services:
  db:
    image: postgres:16
    container_name: aurora-container
    environment:
      POSTGRES_DB: aurora-database
      POSTGRES_USER: aurora
      POSTGRES_PASSWORD: 5e5i_aurora
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:

